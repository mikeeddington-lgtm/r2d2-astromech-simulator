The exact `.ino` sources the three firmware profiles were hand-ported from,
plus Dan Kraus's canonical padawan360 body sketch — fetched from their
public GitHub repos on 2026-08-08. They are the GOLDEN INPUTS for
tests/sketch.test.js: the transpiler must accept each one with zero residue
and, for mod2026, drive the same outputs as the hand port under the same
scripted pad input. They are other people's (public, open-source) sketch
code and are not part of the simulator itself.
